from setuptools import setup

setup(name='bigpy',
    version='0.1',
    description='Make things easier for data analyis and automation',
    packages=['webscrap'],
    install_requires=[
        'imapclient'
    ])
